# MoveItPy configuration for SO-101
# Loaded by moveit_py_test.launch.py → passed as node parameters to MoveItPy

planning_scene_monitor_options:
  name: "planning_scene_monitor"
  robot_description: "robot_description"
  joint_state_topic: "/follower/joint_states"
  attached_collision_object_topic: "/moveit_cpp/planning_scene_monitor"
  publish_planning_scene_topic: "/moveit_cpp/publish_planning_scene"
  monitored_planning_scene_topic: "/moveit_cpp/monitored_planning_scene"
  wait_for_initial_state_timeout: 0.0

planning_pipelines:
  # WSL2 는 OMPL/Pilz 만 (cuMotion 은 cuda-toolkit-13+VPI+GXF 의존성 때문에 Linux 서버에서만).
  # Linux 서버에서 cuMotion 사용 시 아래에 "isaac_ros_cumotion" 추가.
  pipeline_names: ["ompl", "pilz_industrial_motion_planner"]

# Default plan request params (used when arm.plan() is called with no args)
plan_request_params:
  planning_attempts: 1
  planning_pipeline: ompl
  planner_id: "RRTConnect"
  max_velocity_scaling_factor: 1.0
  max_acceleration_scaling_factor: 1.0
  planning_time: 1.0

# ── Named parameter sets (select per-move in Python) ─────────────
ompl_rrtc:
  plan_request_params:
    planning_attempts: 1
    planning_pipeline: ompl
    planner_id: "RRTConnect"
    max_velocity_scaling_factor: 1.0
    max_acceleration_scaling_factor: 1.0
    planning_time: 1.0

pilz_lin:
  plan_request_params:
    planning_attempts: 1
    planning_pipeline: pilz_industrial_motion_planner
    planner_id: "LIN"
    max_velocity_scaling_factor: 1.0
    max_acceleration_scaling_factor: 1.0
    planning_time: 1.0

# cuMotion(cuRobo) GPU planner — primary free-space planner.
# isaac_ros_cumotion_moveit 플러그인이 cumotion_action_server(별도 노드)로 위임.
# planner_id 는 cuMotion plugin 이 노출하는 이름(설치 버전에 따라 "cuMotion" 확인).
cumotion:
  plan_request_params:
    planning_attempts: 1
    planning_pipeline: isaac_ros_cumotion
    planner_id: "cuMotion"
    max_velocity_scaling_factor: 1.0
    max_acceleration_scaling_factor: 1.0
    planning_time: 5.0
